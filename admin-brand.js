(()=>{const pad=n=>String(n).padStart(2,'0');const fmt=d=>d.toLocaleDateString('pt-BR',{day:'2-digit',month:'long'});const fmtFull=d=>d.toLocaleDateString('pt-BR',{weekday:'long',day:'2-digit',month:'long'});function startOfWeek(d=new Date()){const x=new Date(d);x.setHours(12,0,0,0);x.setDate(x.getDate()-x.getDay());return x}function buildWeek(){const weekly=document.getElementById('weekly');if(!weekly||weekly.querySelector('.pastoral-week-head'))return;const start=startOfWeek(),end=new Date(start);end.setDate(start.getDate()+6);const today=new Date();today.setHours(12,0,0,0);const names=['DOM','SEG','TER','QUA','QUI','SEX','SÁB'];const days=Array.from({length:7},(_,i)=>{const d=new Date(start);d.setDate(start.getDate()+i);const cls=d.toDateString()===today.toDateString()?'pastoral-day today':'pastoral-day';return `<div class="${cls}"><small>${names[i]}</small><b>${pad(d.getDate())}</b></div>`}).join('');const head=document.createElement('section');head.className='pastoral-week-head';head.innerHTML=`<div class="week-title"><span class="week-logo" aria-hidden="true"></span><div><small>HOUSES PASTORES</small><h2>Central da Semana</h2><p class="week-range">${fmtFull(start)} a ${fmtFull(end)}</p></div></div><div class="pastoral-days">${days}</div><p class="week-note">Tudo o que for publicado abaixo pertence à semana de <b>${fmt(start)}</b> a <b>${fmt(end)}</b>.</p>`;weekly.insertBefore(head,weekly.firstChild);const grid=weekly.querySelector('.weekly-admin-grid');if(grid){const left=grid.children[0],right=grid.children[1];if(left){const label=document.createElement('div');label.className='weekly-section-label';label.textContent='Publicar nesta semana';left.insertBefore(label,left.firstChild)}if(right){const label=document.createElement('div');label.className='weekly-section-label';label.textContent='Avisos, palavras e materiais desta semana';right.insertBefore(label,right.firstChild)}}const weekInput=document.getElementById('weeklyMaterialWeek');if(weekInput&&!weekInput.value)weekInput.value=`${start.getFullYear()}-${pad(start.getMonth()+1)}-${pad(start.getDate())}`;const commStart=document.getElementById('commStart'),commEnd=document.getElementById('commEnd');if(commStart&&!commStart.value)commStart.value=`${start.getFullYear()}-${pad(start.getMonth()+1)}-${pad(start.getDate())}`;if(commEnd&&!commEnd.value)commEnd.value=`${end.getFullYear()}-${pad(end.getMonth()+1)}-${pad(end.getDate())}`}
function brand(){const header=document.querySelector('header>div:first-child');if(header&&!header.querySelector('.brand-lockup')){const oldStrong=header.querySelector('strong'),oldSpan=header.querySelector('span');if(oldStrong)oldStrong.style.display='none';if(oldSpan)oldSpan.style.display='none';const wrap=document.createElement('div');wrap.className='brand-lockup';wrap.innerHTML='<span class="brand-s" aria-hidden="true"></span><span class="brand-copy"><b>HOUSES</b><em>PASTORES</em><small>LIDERANÇA • UNIDADE • PROPÓSITO</small></span>';header.appendChild(wrap)}}
document.addEventListener('DOMContentLoaded',()=>{brand();buildWeek()});if(document.readyState!=='loading'){brand();buildWeek()}})();