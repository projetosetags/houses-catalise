{
  "name": "houses-catalise",
  "private": true,
  "version": "2.0.0",
  "scripts": {
    "test": "node --test test/*.test.cjs",
    "check": "node scripts/check.cjs",
    "emulators": "firebase emulators:start --project demo-houses --only auth,firestore,storage,functions",
    "deploy:backend": "firebase deploy --only firestore,storage,functions",
    "build:hosting": "node scripts/build-hosting.cjs"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.0",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.1",
    "jsdom": "26.1.0"
  }
}
