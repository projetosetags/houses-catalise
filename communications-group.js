(()=>{function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}function norm(s){return String(s||'').trim().toLocaleLowerCase('pt-BR')}function grouped(list){const groups=[];const map=new Map();for(const c of list||[]){const key=norm(c.title)||'comunicacoes';let g=map.get(key);if(!g){g={title:c.title||'Comunicações',items:[]};map.set(key,g);groups.push(g)}g.items.push(c)}return groups}window.renderCommunications=function(target,list,compact=false){const box=document.querySelector(target);if(!box)return;const groups=grouped(list);box.innerHTML=groups.length?groups.map(g=>`<article class="comm-card comm-group"><small>COMUNICAÇÕES</small><b>${esc(g.title)}</b><div class="comm-lines">${g.items.map(c=>`<p><span class="comm-dot">•</span>${esc(c.message)}</p>`).join('')}</div></article>`).join(''):(compact?'<div class="comm-card"><p>Nenhum comunicado para esta semana.</p></div>':'')};if(typeof loadPublic==='function')loadPublic()})();